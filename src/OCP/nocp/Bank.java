package OCP.nocp;

/**
 * 개방 폐쇄의 원칙
 * 기능확장에는 열려있고, 기능 수정에는 닫혀있어야한다
 * 데이터를 직접주는게 아닌, 기능으로 제공하면됩니다.
 */
public class Bank {
    BankUser bankUser = new BankUser();
    public void checkPromotion(){
        if(bankUser.canVIP()){
            // bankUser VIP 선정
        } if(bankUser.canVIP()){
            // bankUser VIP 선정
        } if(bankUser.canVIP()){
            // bankUser VIP 선정
        } if(bankUser.canVIP()){
            // bankUser VIP 선정
        } if(bankUser.canVIP()){
            // bankUser VIP 선정
        } if(bankUser.canVIP()){
            // bankUser VIP 선정
        } if(bankUser.canVIP()){
            // bankUser VIP 선정
        } if(bankUser.canVIP()){
            // bankUser VIP 선정
        } if(bankUser.canVIP()){
            // bankUser VIP 선정
        } if(bankUser.canVIP()){
            // bankUser VIP 선정
        } if(bankUser.canVIP()){
            // bankUser VIP 선정
        } if(bankUser.canVIP()){
            // bankUser VIP 선정
        } if(bankUser.canVIP()){
            // bankUser VIP 선정
        } if(bankUser.canVIP()){
            // bankUser VIP 선정
        }


    }
}
