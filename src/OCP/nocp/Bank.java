package OCP.nocp;

public class Bank {
    BankUser bankUser = new BankUser();
    public void checkPromotion(){
        if(bankUser.canVIP()){
            // bankUser VIP 선정
        } if(bankUser.canVIP()){
            // bankUser VIP 선정
        } if(bankUser.canVIP()){
            // bankUser VIP 선정
        } if(bankUser.canVIP()){
            // bankUser VIP 선정
        } if(bankUser.canVIP()){
            // bankUser VIP 선정
        } if(bankUser.canVIP()){
            // bankUser VIP 선정
        } if(bankUser.canVIP()){
            // bankUser VIP 선정
        } if(bankUser.canVIP()){
            // bankUser VIP 선정
        } if(bankUser.canVIP()){
            // bankUser VIP 선정
        } if(bankUser.canVIP()){
            // bankUser VIP 선정
        } if(bankUser.canVIP()){
            // bankUser VIP 선정
        } if(bankUser.canVIP()){
            // bankUser VIP 선정
        } if(bankUser.canVIP()){
            // bankUser VIP 선정
        } if(bankUser.canVIP()){
            // bankUser VIP 선정
        }


    }
}
